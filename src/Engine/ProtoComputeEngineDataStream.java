package engine;

/**
 * Data stream that gets passed inside of the compute engine itself
 */
public interface ProtoComputeEngineDataStream {
    // will hold the integer in some format parsable by the computing part of the compute engine
}