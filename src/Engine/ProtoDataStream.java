package engine;

/**
 * Data stream that gets passed around (will be an integer) between the user and the compute engine
 */
public interface ProtoDataStream {
    // will hold the integer in some format parsable by both the user and compute engine initializer
	
}